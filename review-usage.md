# 怎么用

## 使用方法

常驻，不需要 @skill，也不需要用户或关键词触发。

## 功能

### 打开 Review

#### 命令

| 命令 | 说明 |
|------|------|
| `/skill-review` | 打开 Skills 总览 |
| `/<skill> /skill-review` | 只体检该 skill，再打开它的单页 |
| `/skills-check` | 打开 Skills 总览（同单独 `/skill-review`） |

#### 工具

| 工具 | 说明 |
|------|------|
| 需关注/缺件条目上的「开对话」 | 弹出预填对话，确认发送后开始修；不会自动执行 |
| 卡片右键 →「开对话修复」 | 该 skill 有需关注/缺件时出现，弹出预填对话 |
| 需关注条目上的「忽略」 | 记下这一条，之后总览和单页都不再列出 |

### 在总览页管标签

#### 工具

| 工具 | 说明 |
|------|------|
| 标签下拉 | 勾选一个或多个标签，带其中任一标签的 skill 会显示 |
| 清除筛选 | 有状态、类型或标签筛着时出现，一点还原成全部 |
| 卡片右键 →「编辑标签」 | 弹窗里删标签、点已有标签加入、输入框回车新建，保存即写入 |

### 在总览页管类型

#### 工具

| 工具 | 说明 |
|------|------|
| 类型筛选 | 点「演进型」或「底本型」，只看该类；再点取消 |
| 卡片右键 →「标为演进型」/「标为底本型」 | 写入该 skill 的 `kind.txt` 并刷新 |

### 接入 Review

#### 命令

- **说**：给这个 skill 加上 Review
