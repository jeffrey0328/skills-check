# 怎么用

## 使用方法

常驻，不需要 @skill，也不需要用户或关键词触发。

## 功能

### 打开 Review

#### 命令

| 命令 | 说明 |
|------|------|
| `/skills-check` | 打开 Skills 总览 |
| `/<skill> -review` | 打开该 skill 单页 |

### 在总览页管标签

#### 工具

| 工具 | 说明 |
|------|------|
| 标签下拉 | 勾选一个或多个标签，带其中任一标签的 skill 会显示 |
| 清除筛选 | 有状态或标签筛着时出现，一点还原成全部 |
| 卡片右键 →「编辑标签」 | 弹窗里删标签、点已有标签加入、输入框回车新建，保存即写入 |

### 接入 Review

#### 命令

- **说**：给这个 skill 加上 Review
